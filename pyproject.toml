[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "neutral-selection"
version = "0.1.0"
description = "Neutral Selection representation, variation and selection framework"
requires-python = ">=3.8"
dependencies = []
